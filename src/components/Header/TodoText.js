import Classes from '../../CSS/main/TodoText.module.css';

const TodoText = () => {
    return (
        <h1 className={Classes.TodoText}>
            <span>To</span>Do
        </h1>
    );
};

export default TodoText;
